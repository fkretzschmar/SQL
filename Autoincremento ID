#Autoincremento geralmente utilizado em id pois a linha de baixo sera somada +1.


CREATE TABLE tab_identity
(
id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
descritor VARCHAR(20),
PRIMARY KEY(id)
);
